node_modules/
# Copied in from ../renderer at build time. One source of truth lives there;
# a checked-in copy here would be a second one to forget to update.
www/app/
# Build output, not sources.
android/app/build/
android/build/
android/.gradle/
android/local.properties
ios/App/build/
ios/App/Pods/
ios/App/App/public/

# Drawn at build time by scripts/make-icons.js from the same glyph the desktop
# uses. Committing it would be a second copy of the artwork to keep in step.
ios/App/App/Assets.xcassets/AppIcon.appiconset/
