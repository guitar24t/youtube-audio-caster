{
  "name": "youtube-audio-caster-mobile",
  "version": "1.5.1-a",
  "private": true,
  "description": "Phone control surface for YouTube Audio Caster. Talks to the desktop app over the local network; it is not a second copy of it.",
  "scripts": {
    "sync:www": "node scripts/sync-www.js",
    "sync": "npm run sync:www && cap sync",
    "build:android": "npm run sync:www && cap sync android && cd android && ./gradlew assembleDebug",
    "build:ios": "npm run sync:www && cap sync ios",
    "check": "node scripts/check-www.js"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/core": "8.5.1",
    "@capacitor/ios": "8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1"
  }
}
